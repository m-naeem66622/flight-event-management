<!DOCTYPE html>
<html>
  <head>
    <title>Reservation Error</title>
    <link rel="stylesheet" href="/styles/main.css" />
  </head>

  <body>
    <header>
      <img src="/images/airplane-header-image.jpg" alt="Airline" />
      <h1>Airline Ticket Reservation</h1>
    </header>
    <main>
      <div class="message">
        <h2>Unable to Make Reservation</h2>
        <p>Please check the following errors:</p>

        <ul>
          <% if (locals.errors) { %> <% errors.forEach(error => { %>
          <li><%= error %></li>
          <% }) %> <% } %>
        </ul>
      </div>

      <a href="/" class="button">Back to Reservation Form</a>
    </main>
  </body>
</html>
